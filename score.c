#include <stdio.h>
#include "board.c"
#include "player.c"

int multiply(int letter_value, int tiles_count, int square_value, char square_flag)
{
    int sum = 0;
    if (square_flag == 'W')
    {

    }
    else if (square_flag == 'L')
    {

    }
    else
    {
        
    }
    return sum;
}